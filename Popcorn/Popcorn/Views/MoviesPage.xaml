<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Popcorn.Views.MoviesPage"
             Title="Movies"
             Visual="Material">
    <ContentPage.Content>
        <StackLayout>
            <Grid VerticalOptions="End" Margin="-2, 0">
                <Editor HeightRequest="50" Margin="2" x:Name="eSearch" Grid.Row="0" Grid.ColumnSpan="2" Placeholder="Search"></Editor>
                <Picker HeightRequest="50" Margin="2" Grid.Column="0" Grid.Row="1" x:Name="pickSort" SelectedIndexChanged="pickSort_SelectedIndexChanged">
                    <Picker.Items>

                    </Picker.Items>
                </Picker>
                <Button HeightRequest="50" Margin="2" x:Name="btnSearch" Grid.Column="1" Grid.Row="1" Text="Search" Clicked="btnSearch_Clicked"></Button>
            </Grid>

            <Grid VerticalOptions="Start">
                <CarouselView HeightRequest="350" x:Name="cvContent" PeekAreaInsets="0" NumberOfSideItems="0" CurrentItemChanged="cvContent_CurrentItemChanged" VerticalOptions="Start" HorizontalOptions="Start" ItemSizingStrategy="MeasureFirstItem" >
                    <CarouselView.ItemTemplate>
                        <DataTemplate>
                            <ContentView>
                                <Image Aspect="AspectFit" Source="{Binding Images.Poster.Uri.OriginalString}"></Image>
                            </ContentView>
                        </DataTemplate>
                    </CarouselView.ItemTemplate>
                </CarouselView>
            </Grid>

            

            <Grid VerticalOptions="Start" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" HorizontalOptions="Fill" x:Name="lblTitle" Text="title" TextColor="Black" FontSize="Title"></Label>
                <Label Grid.Row="1" HorizontalOptions="Fill" x:Name="lblYear" Text="year" FontSize="Body"/>
                <Label Grid.Row="3" HorizontalOptions="Fill" x:Name="lblRuntime" Text="runtime" FontSize="Body"/>
                <Label Grid.Row="2" HorizontalOptions="Fill" x:Name="lblSynopsis" Text="synopsis" FontSize="Body"/>
            </Grid>

            <Grid VerticalOptions="EndAndExpand" HeightRequest="65" Margin="-4">
                <Button Grid.Column="0" x:Name="btnTrailer" Clicked="btnTrailer_Clicked" Text="Trailer" Margin="-2, 0" CornerRadius="0"></Button>
                <Button Grid.Column="1" x:Name="btnDownload" Clicked="btnDownload_Clicked" Text="Download" Margin="-2, 0" CornerRadius="0"></Button>
            </Grid>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>
